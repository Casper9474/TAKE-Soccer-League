info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/goal/:id
  body:
    type: json
    data: |-
      {
        "match": { "id": 1 },
        "player": { "id": 3 },
        "minute": 15,
        "goalType": "PENALTY"
      }
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
