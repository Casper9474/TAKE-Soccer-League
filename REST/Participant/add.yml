info:
  name: add
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/participant
  body:
    type: json
    data: |-
      {
        "season": { "id": 2 },
        "club": { "id": 3 },
        "points": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
