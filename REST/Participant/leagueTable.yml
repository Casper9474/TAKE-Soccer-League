info:
  name: leagueTable
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/participant/league-table/:seasonId
  params:
    - name: seasonId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5