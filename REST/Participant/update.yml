info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/participant/:id
  body:
    type: json
    data: |-
      {
        "season": { "id": 1 },
        "club": { "id": 1 },
        "points": 89
      }
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
