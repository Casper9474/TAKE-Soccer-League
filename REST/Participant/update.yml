info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/participant
  body:
    type: json
    data: |-
      {
        "id": 1,
        "season": { "id": 1 },
        "club": { "id": 1 },
        "points": 89
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
