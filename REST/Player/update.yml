info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/player/:id
  body:
    type: json
    data: |-
      {
        "club": { "id": 1 },
        "firstName": "Kylian",
        "lastName": "Mbappé",
        "number": 10,
        "birthDate": "1998-12-20",
        "position": "FORWARD"
      }
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
