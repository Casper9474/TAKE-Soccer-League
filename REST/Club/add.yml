info:
  name: add
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/club
  body:
    type: json
    data: |-
      {
        "name": "Real Madrid",
        "city": "Madrid",
        "foundedYear": 2000,
        "stadiumName": "Benz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
