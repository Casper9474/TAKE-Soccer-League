info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/club/:id
  body:
    type: json
    data: |-
      {
        "name": "Real Madrid",
        "city": "Madrid",
        "foundedYear": 2000,
        "stadiumName": "Benz"
      }
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
