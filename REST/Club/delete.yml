info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/club/:id
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
