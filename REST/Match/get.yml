info:
  name: get
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/match/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
